<Window x:Class="BreakTimer.TimeOutWindow"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BreakTimer"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Break Time!" Height="160" Width="363">
    <Grid Height="auto" Width="auto">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Name="textBlock" TextAlignment="Center" VerticalAlignment="Center" Width="auto" FontSize="16">
            Time out! <LineBreak/>
            Take a break.
        </TextBlock>
        <Button Grid.Row="1" Name="closeButton" Content="OK" IsDefault="True" HorizontalAlignment="Center" VerticalAlignment="Center" Width="84" Height="auto" Click="CloseButton_Click"/>
    </Grid>
</Window>
